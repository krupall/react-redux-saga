import { PLAYER_LIST, TEAM_LIST } from "./constant"

export const playerlist = () => {
  
    return {
        type: PLAYER_LIST,
    }
}

export const teamlist = () => {
  
    return {
        type: TEAM_LIST,
    }
}
